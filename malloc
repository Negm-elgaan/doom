#include <bits/stdc++.h>
#include <stdlib.h>

using namespace std;

int main()
{
    int x;
    int *p = (int *) malloc(sizeof(int));
    cin>>x;
    p = &x;
    cout<<*p;
    free(p);
    return 0;
}
